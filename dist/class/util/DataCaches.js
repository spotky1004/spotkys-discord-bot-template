export default class DataCaches {
    constructor(app, data) {
        this.app = app;
        this.defaultData = data;
        this.cache = {};
    }
    async fetchData(documentId) {
        const data = ;
    }
}
